services:
  - type: worker           # было web — меняем на worker
    name: mindmeld-telegram-bot
    env: python
    region: frankfurt
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
